# Web Client

This directory is reserved for the fresh browser-game install.

Recommended starting stack:

- TypeScript
- Vite
- three.js
- glTF/GLB asset pipeline

The full feasibility report lives in `docs/reports`.
